import { IRadioButtons } from "./RadioButtons";

const base: IRadioButtons = {
  selectedOption: "",
  setSelectedOption: () => {},
  options: [],
};

export const mockRadioButtonsProps = {
  base,
};
