import { IRouterButton } from "./RouterButton";

const base: IRouterButton = {
  wrapperStyles: "",
  buttonStyles: "p-4 text-white font-semibold text-lg",
  buttonLabel: "X",
  route: "",
};

export const mockRouterButtonProps = {
  base,
};
